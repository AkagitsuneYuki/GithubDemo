using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Present : MonoBehaviour
{
    private object ass;
    // Start is called before the first frame update
    void Start()
    {
        GetSumAss();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public object GetSumAss()
    {
        return ass;
    }
}
